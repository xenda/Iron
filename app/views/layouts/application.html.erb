<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:fb="http://www.facebook.com/2008/fbml">
<head>
  <title>Kato</title>
  <link href='http://fonts.googleapis.com/css?family=Droid+Serif:regular,bold&v1' rel='stylesheet' type='text/css'>
  <%= include_stylesheets :app %>
  <%= csrf_meta_tag %>
</head>
<body>
  
  <div id="header">
    <div id="logo">
      <a href="http://bbvacontinental.com" target="_blank">BBVA Continental</a>
    </div>
    <div id="contest">
      <a href="/">Tercer Concurso Perú Gourmet</a>
    </div>

  </div>
  
  <div class="wrapper">
    
    <%= yield %>    

  </div>
  <div id="fb-root"></div>
  <script src="http://connect.facebook.net/en_US/all.js"></script>
  <script>
  FB.init({
  appId : '136578643087393',
  status : true, // check login status
  cookie : true, // enable cookies to allow the server to access the session
  xfbml : true // parse XFBML
  });
  </script>
  <%= include_javascripts :app %> 
  <script src="http://js.pusherapp.com/1.8/pusher.min.js" type="text/javascript"></script>
  <script type="text/javascript">
    window.fbAsyncInit = function() {

      FB.init({
      appId : '110963292340399',
      status : true, // check login status
      cookie : true, // enable cookies to allow the server to access the session
      xfbml : true,
      oauth : true //
      });

      window.setTimeout(function() {
        FB.Canvas.setAutoResize();
      }, 250);

      window.fbAsyncInit = function() {
        FB.Canvas.setSize();
      }
      // Do things that will sometimes call sizeChangeCallback()
      function sizeChangeCallback() {
        FB.Canvas.setSize();
      }


    };

        FB.getLoginStatus(function(response) {
       if (response.session) {
         // console.log('got session, ok to post');
       } else {
      FB.login(function(response) {
        if (response.session) {
          // console.log('user logged in');
        } else {
          // console.log('canceled login');
        }
      });
        }
      });

    (function() {
      var e = document.createElement('script'); e.async = true;
      e.src = document.location.protocol +
        '//connect.facebook.net/en_US/all.js';
      document.getElementById('fb-root').appendChild(e);
    }());
  </script>
</body>
</html>